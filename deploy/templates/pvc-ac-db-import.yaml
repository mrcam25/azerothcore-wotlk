apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Values.images.ac_db_import.name }}-pvc"
  labels:
    app: {{ .Values.app.name }}
    component: {{ .Values.images.ac_db_import.name }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
